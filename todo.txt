/*
 * Module code goes here. Use 'module.exports' to export things:
 * module.exports.thing = 'a thing';
 *
 * You can import it from another modules like this:
 * var mod = require('todo');
 * mod.thing == 'a thing'; // true
 */


/* TODO:

Figure out how to import settings in one central modules to others
Alert with NPC owners' name when attack occurs
Patch all harvesting roles to move away from any tile adjacent to a source
Fix WallRepairer to function
Create Announcer role to stand still and diaply status messages
Create raid() function
Create attack / tank / healer roles
Patch builders to also repair ramparts to 5% once built
Make census a separate module
Expand announcer in main.js to take in creep names
Make certain settings vary over time to simulate what i do manually (altering ratios of creeps produced essentially)
*/
module.exports = {

};